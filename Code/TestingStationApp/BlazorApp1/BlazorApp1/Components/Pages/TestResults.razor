@page "/testresults"
@using System.Collections.Generic
@rendermode InteractiveServer

<h1>Test results</h1>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-6 overflow-auto" style="height: 100vh; border-right: 2px solid #dee2e6;">
            @foreach (var test in allTests)
            {
                        <div class="card mb-3" style="width: 18rem;" @onclick="() => SelectTest(test)">
                        <div class="card-body">
                                <h5 class="card-title">@test.testID</h5>
                                    <h4 class="card-title">@test.testName</h4>
                               
                        </div>
                    </div>
            }
        </div>

        <div class="col-md-6">
            @if (SelectedTest.HasValue)
            {
                    <div>
                        <h3>@SelectedTest.Value.testName : @testName</h3>
                        <p>Format: @SelectedTest.Value.format</p>
                        <p>Algorithm: @SelectedTest.Value.testedAlgorithm</p>
                        <p>ID: @SelectedTest.Value.testID</p>
                        <p>Degree @SelectedTest.Value.numberOfPointsPerSegment</p>
                    </div>
            }
        </div>
    </div>
</div>

